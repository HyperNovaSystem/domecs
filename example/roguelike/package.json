{
  "name": "@domecs-example/roguelike",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "DOMECS v0.1 exemplar — headless turn-based roguelike (SPEC exemplar #1).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "tsc --noEmit && vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "domecs": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
