{
  "name": "@domecs-example/restaurant",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "DOMECS v0.1 exemplar — small restaurant simulation (customers, waiters, tables, tips).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "tsc --noEmit && vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "domecs": "workspace:*",
    "domecs-dom": "workspace:*",
    "domecs-input": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.9"
  }
}
